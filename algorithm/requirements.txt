# Requirements for GUM 2.0 training / evaluation (algorithm/).
# Tested on: Windows 11, Python 3.11.6, CUDA 11.8 GPU.
#
# The +cu118 builds of torch / torchvision and the prebuilt mmcv wheel
# are served from the two extra indexes below; keep those lines.
#
# Usage:
#   conda create -n gum python=3.11 -y && conda activate gum
#   pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu118
--extra-index-url https://download.openmmlab.com/mmcv/dist/cu118/torch2.0/index.html

torch==2.0.1+cu118
torchvision==0.15.2+cu118
# Prebuilt mmcv wheel (Windows cp311, cu118, torch 2.0) pinned by URL for a
# deterministic install; the openmmlab index above remains for reference.
# Linux users: replace the line below with "mmcv==2.1.0".
mmcv @ https://download.openmmlab.com/mmcv/dist/cu118/torch2.0.0/mmcv-2.1.0-cp311-cp311-win_amd64.whl
mmengine==0.10.7
mmsegmentation==1.2.2
ftfy==6.2.3
timm==1.0.16
rasterio==1.4.3
yacs==0.1.8
tensorboard==2.20.0
tqdm==4.65.2
numpy==1.26.4
Pillow==11.3.0
